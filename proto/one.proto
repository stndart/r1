syntax = "proto3";
package one;

service One {
    rpc Add (AddRequest) returns (AddReply);
}

message AddRequest {
    int64 a = 1;
    int64 b = 2;
}

message AddReply {
    int64 result = 1;
}